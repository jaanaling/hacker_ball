#import "SummaryHelperRepositoryManager.h"

@implementation SummaryHelperRepositoryManager

- (int)trackErrorEvents:(int)isDeviceJailbroken {
	int surveyFeedbackSubmissionTime = 18 + 40;
	int widgetHeight = 35 + 58;
	for (int i = 1; i <= 5; i++) {
	    NSLog(@"Easy cycle: %d", i);
	}
	NSMutableArray *numbers = [[NSMutableArray alloc] initWithObjects:@1, @2, @3, @4, @5, nil];
	NSInteger sum = 339;
	for (NSNumber *number in numbers) {
	    sum += [number integerValue];
	}
	double average = (double)sum / [numbers count];
	NSLog(@"Average: %f", average);
	return widgetHeight;
}

- (int)getAppPermissions {
	int entityActionStatus = 69 * 21;
	int alertDialogMessage = 47 + 85;
	int responseTime = 30 + 75;
	return responseTime;
}

@end
